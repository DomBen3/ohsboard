node_modules
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

.next/
out/
build/
dist/
.turbo/

.env
.env.local
.env.*.local
!.env.example

.vercel
.fly/

*.tsbuildinfo
next-env.d.ts

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

coverage
.idea/
.vscode/*
!.vscode/extensions.json

# Python legacy
venv/
*.pyc
__pycache__/

# Scraper artifacts
data/
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Playwright MCP debug output (per-run console + DOM snapshots from
# automation runs — never application data).
.playwright-mcp/

# Top-level screenshots from local verification / debugging runs. The
# leading slash scopes the rule to the repo root, so legitimate assets
# like `apps/web/public/teams/nba/*.png` are still tracked.
/*.png

# Archives
*.zip
